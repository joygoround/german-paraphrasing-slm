# Core training stack (versions used during training — Table 4.2)
torch==2.10.0          # ROCm build: pip install torch --index-url https://download.pytorch.org/whl/rocm6.2
transformers==5.3.0
datasets==4.4.1
accelerate==1.12.0
bitsandbytes==0.49.0   # 8-bit AdamW optimizer (AllComb and Contrastive strategies)

# Evaluation — reference-based metrics
bert-score
sacrebleu
evaluate

# Evaluation — surface & structural metrics
textstat
spacy                  # model: python -m spacy download de_core_news_lg

# Evaluation — learned metric
# DETECT must be installed from source: https://github.com/ZurichNLP/DETECT
# After cloning, set DETECT_PATH in eval_detect.py accordingly.

# Evaluation — LLM-as-a-Judge
openai
python-dotenv

# General
scikit-learn
pandas
numpy
tqdm